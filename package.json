{
  "name": "wedding",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "web-dev-server --node-resolve --watch --open",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf public && mkdir public && cp -r js assets css fullPage.js-master index.html public/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@web/dev-server": "^0.1.29"
  }
}
